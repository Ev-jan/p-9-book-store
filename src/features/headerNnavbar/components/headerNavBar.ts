import { navigationItems } from "../model/HeaderNavBar";
import { Navigation } from "../../../shared/ui/navigation/navigation";
import * as styles from "./headerNavBar.scss"

export const HeaderNavBar = Navigation(navigationItems);